apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/instance: chatbot-test-database
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: chatbot-test-database
  name: chatbot-test-database
spec:
  ports:
    - port: 
      protocol: TCP
      targetPort: 
  selector:
    app.kubernetes.io/instance: chatbot-test-database
